DELIMITER $

CREATE OR REPLACE TRIGGER trg_pedido_cliente
AFTER INSERT
ON PEDIDO
FOR EACH ROW

BEGIN 
	UPDATE CLIENTE SET QTD_PEDIDO = QTD_PEDIDO + 1
    	WHERE CLIENTE.COD_CLIENTE = NEW.COD_CLIENT;
        
END $
