DELIMITER $

CREATE OR REPLACE TRIGGER trg_cancela_pedido
AFTER UPDATE
ON PEDIDO
FOR EACH ROW

BEGIN 

     IF NEW.SN_CANCELADO = 'S' THEN 
	
	UPDATE CLIENTE SET QTD_CANCELADO = QTD_CANCELADO + 1
    	WHERE CLIENTE.COD_CLIENTE = NEW.COD_CLIENTE;

     END IF;
        
END $
